# mechtaev.com

Build the site with [uv](https://docs.astral.sh/uv/):

```sh
make site      # build into _site/
make serve     # build and serve at http://localhost:8000
make clean     # remove _site/
```
